spring:
  datasource:
    url: jdbc:mysql://shipping-db:3306/shipping_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
  kafka:
    bootstrap-servers: kafka:29092
    consumer:
      group-id: delivery-service-group
  data:
    redis:
      host: redis
      port: 6379
